query repoSummary($org:String!) {
  repositoryOwner(login: $org) {
    repositories(first:100, affiliations: OWNER, orderBy: {field: PUSHED_AT, direction: DESC}, isFork: false) {
      nodes {
        owner {login},
        repo:name,
        open_issues : issues(first:100, states:OPEN) {
          totalCount
          nodes {
            number
            title
            updatedAt
            p1 : reactions(content:THUMBS_UP) {
              totalCount
            }
            labels(first:10) {
              nodes {
                name
              }
            }
          }
          pageInfo {
            endCursor
            hasNextPage
          }
        }
        prs : pullRequests(states:OPEN) {
          totalCount
        }
        watchers : stargazers() {
          totalCount
        }
        DESCRIPTION : object(expression: "master:DESCRIPTION") {
          ... on Blob {
            text
          }
        }
      }
    }
  }
}
